"use client";

const BackgroundCanvas = () => {
  return (
    <canvas
      id="bgCanvas"
      className="absolute top-0 left-0 w-full h-full"
    ></canvas>
  );
};

export default BackgroundCanvas;
